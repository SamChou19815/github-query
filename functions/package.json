{
  "name": "github-query-functions",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "tsc",
    "build:deploy": "ncc build index.ts -o build -m",
    "clean": "rm -rf build"
  },
  "files": [
    "*.js",
    "*.d.ts",
    "*.json"
  ],
  "main": "index.js",
  "dependencies": {
    "firebase-functions": "^3.3.0",
    "github-query-backend": "0.1.0"
  }
}
