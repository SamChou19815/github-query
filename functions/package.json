{
  "name": "github-query-functions",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "prebuild": "yarn workspace github-query-backend build",
    "build": "tsc",
    "build:deploy": "yarn build && ncc build index.js -o build -m",
    "clean": "rm -rf build"
  },
  "files": [
    "*.js",
    "*.d.ts",
    "*.json"
  ],
  "main": "index.js",
  "dependencies": {
    "firebase-functions": "^3.3.0",
    "github-query-backend": "0.1.0"
  }
}
